--- a/SKILL.md
+++ b/SKILL.md
@@ -2,6 +2,11 @@
 name: video-use
 description: Edit any video by conversation. Transcribe, cut, color grade, generate overlay animations, burn subtitles — for talking heads, montages, tutorials, travel, interviews. No presets, no menus. Ask questions, confirm the plan, execute, iterate, persist. Production-correctness rules are hard; everything else is artistic freedom.
 ---
+<!-- LOCAL: dependency note -->
+> Transcription here calls ElevenLabs Scribe and needs a paid `ELEVENLABS_API_KEY`. On this machine
+> secrets live in Doppler, not in a `.env` beside the repo: run the helpers under
+> `doppler run --project <project> --config prd --` so the key is injected, and never write a key to disk.
+<!-- LOCAL END -->
 
 # Video Use
 
